//TODO: Print the list of cards as string, separated by space

function printDeckOfCards(cards) {
    function createCard (){
       // Use the solution from the previous task 
    }
      // TODO
  }

// Input:
printDeckOfCards(['AS', '10D', 'KH', '2C'])     // A♠ 10♦ K♥ 2♣
printDeckOfCards(['5S', '3D', 'QD', '5X'])      // Invalid card: 5X